// mkdir foundry-nft
// cd foundry-nft
// forge init

`forge install OpenZeppelin/openzeppelin-contracts`


`forge install Cyfrin/foundry-devops`

## svg ??

SVG stands for Scalable Vector Graphics.
 In 'simple' terms, SVG is a way to define images in a two-dimensional space using XML coded tags with specific parameters.

when you have `svg of image`.// code

`base64 -i sad.svg` --> command
/get URL

data:image/svg+xml;base64,  (adding this portion for that URL)

then paste the whole in any website.

## Dynamic NFT Collection

created MoodNft file in src.
